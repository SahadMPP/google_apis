import 'package:flutter/material.dart';

class GooglePaymentScreen extends StatelessWidget {
  const GooglePaymentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Google payment"),
      ),
    );
  }
}