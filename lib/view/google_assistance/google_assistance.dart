import 'package:flutter/material.dart';

class GoogleAssistanceScreen extends StatelessWidget {
  const GoogleAssistanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Google Assist"),
      ),
    );
  }
}