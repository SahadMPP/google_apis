import 'package:flutter/material.dart';

class GooglePlacePikingScreen extends StatelessWidget {
  const GooglePlacePikingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Google Place Piking"),
      ),
    );
  }
}