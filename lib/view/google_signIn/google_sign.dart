import 'package:flutter/material.dart';

class GoogleSignScreen extends StatelessWidget {
  const GoogleSignScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Google Sign"),
      ),
    );
  }
}